### 🚀 Cài đặt thư viện

```bash
npm install multer        # Thư viện upload file
npm install bcryptjs      # Thư viện mã hóa mật khẩu
npm install axios         # Gửi HTTP request từ client
npm install nodemailer    # Gửi email qua SMTP
npm install               # Cài đặt tất cả các package từ package.json
### 🌱 Khởi tạo dữ liệu mẫu
npm run seed
